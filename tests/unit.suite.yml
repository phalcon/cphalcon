# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
  config:
    Redis:
      database: '%TEST_RS_DB%'
      host: '%TEST_RS_HOST%'
      port: '%TEST_RS_PORT%'
  enabled:
    - Asserts
    - Filesystem
    - Redis
    - Helper\Unit
